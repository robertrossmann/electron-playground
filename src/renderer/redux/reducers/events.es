const initial = []

/**
 * Events reducer
 *
 * @param     {Array}     state     Initial state for the `events` state node
 * @param     {Object}    action    The triggered action
 * @return    {Array}
 */
export default function events(state = initial, action) {
  switch (action.type) {
    default:
      return state
  }
}
